# Copy this file to .env.local and fill in the keys when ready.
# Leaving any key empty makes the orchestrator skip that provider and
# fall back to the next one (or to the local template engine).

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
